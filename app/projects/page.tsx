import React from "react";
import Base from "@components/layouts/base";
import ProfileHeader from "@components/ui/header";

export const metadata = {
  title: "Projects",
};

export default function AboutMe() {
  return <Base pageSlug="/projects"></Base>;
}
